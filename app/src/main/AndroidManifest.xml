<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.digital.strategy.followup" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo_version"
        android:label="@string/app_name"
        tools:replace="android:icon" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustResize" >

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <!--
        <activity
            android:name=".FollowUpOnePageOne"
            android:label="@string/title_activity_follow_up_one_page_one" >
        </activity>
        <activity
            android:name=".FollowUpOnePageTwo"
            android:label="@string/title_activity_follow_up_one_page_two" >
        </activity>
        <activity
            android:name=".ScreenParagraphActivity"
            android:label="@string/title_activity_screen_kparagraph" >
        </activity>
        -->
        <activity
            android:name=".SessionActivity"
            android:label="@string/title_activity_session"
            android:theme="@style/AppTheme.Base.FullScreen">
        </activity>
        <activity
            android:name=".SelectUser"
            android:label="@string/title_activity_select_user"
            android:theme="@style/AppTheme.Base.FullScreen">
        </activity>
        <activity
            android:name=".LauncherActivity"
            android:label="@string/title_activity_launcher"
            android:theme="@style/AppTheme.Base.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".WebChatrtcManager">
        </service>

    </application>

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <uses-feature android:name="android.hardware.camera" android:required="true"/>
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

</manifest>
